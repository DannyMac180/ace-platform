# Hosted Dashboard Shim

The hosted ACE dashboard implementation no longer lives in this public
repository.

Canonical home:

- `ace-private/apps/cloud-dashboard/`

This directory remains only as a compatibility marker so older docs and links
can point contributors at the correct repo after the cutover cleanup.
