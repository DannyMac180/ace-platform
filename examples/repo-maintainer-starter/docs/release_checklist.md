# Release Checklist

- Draft the release note before tagging a version.
- Confirm every example command runs from the repo root.
- Update contributor docs when a setup step or required tool changes.
- Re-run the local benchmark after workflow changes that affect maintainer
  guidance.
