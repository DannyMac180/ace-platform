# CLI Extension Example

Review this file before adding a new command to the starter project.

- Put parsing and user-facing output in the CLI layer only.
- Move reusable logic into a library module instead of the command entrypoint.
- Add one happy-path test and one failure-path test for every new command.
