# Issue Triage Example

- Reproduce the problem before changing code or docs.
- Link the failing command, file path, or user-visible behavior in the work log.
- Document any follow-up bug separately instead of expanding the current scope.
